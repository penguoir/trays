<% content_for :active_pane do %>
  <%= render "form", project: @project %>
  <%= button_to "Delete Project", project_path(@project), method: :delete, data: { confirm: "Are you sure?" }, class: "border border-black px-2 py-1" %>
<% end %>

<%= render template: "projects/layout" %>
